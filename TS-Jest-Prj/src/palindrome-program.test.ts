import { isPalindrome } from '../palindrome-program'

test('Verify for positive integer factorial', () => {
    const input = 'madam';
    const expectedOutput = true;
    expect(isPalindrome(input)).toBe(expectedOutput);
  });